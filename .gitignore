# Secrets / credentials — never commit these
.env
.env.*
*.pem
*.key
*.secret
credentials.json
vault_password

# OS / editor cruft
.DS_Store
*.swp
*~

# engineering-team run artifacts — working state, not deliverables.
# The skill writes evaluation reports, plans, and progress dashboards here.
.engineering-team/
